<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hruit.mybatis.test.dao.IUserMapper">

    <select id="queryUserInfoById" resultType="user">
        SELECT id, user_id, user_name, user_head, create_time
        FROM user
        where id = #{id}
    </select>
    <select id="queryUserInfoByInfo" resultType="user">
        SELECT id, user_head, user_name, create_time
        FROM user
        where id = #{id}
          and user_id = #{userId}
    </select>
    <select id="selectUserCount" resultType="java.lang.Long">
        SELECT count(*)
        FROM user
        where id = #{id}
    </select>
    <insert id="addUser">
        insert into user (user_id, user_name, user_head, create_time, update_time)
        values (#{userId}, #{userName}, #{userHead}, now(), now());
    </insert>

</mapper>
